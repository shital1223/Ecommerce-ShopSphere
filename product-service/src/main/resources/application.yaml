spring:
  application:
    name: product-service
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh