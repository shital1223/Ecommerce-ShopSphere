spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8084
